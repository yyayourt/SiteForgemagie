# Errata des rapports de `docs/knowledge/`

Les rapports de ce dossier, dont le rapport externe `2026-09-07-croisement-densites-reliquat.md` (abrégé `X`), sont conservés tels quels (règle : ne pas modifier les documents sources). Les corrections établies depuis leur rédaction sont consignées ici.

| Date | Rapport concerné | Affirmation d'origine | Correction | Statut de la correction |
|---|---|---|---|---|
| 2026-09-10 | corpus des probabilités ; `empirical_params.json → probability.ecShare`, `officialFactorsLinear.a/b` | Les triplets « 66/34/0, 43/50/7, 15/50/35 » viennent « d'une seule source ancienne recopiée » et « ne sont pas utilisables » (R PARTIE 3 et Caveats) ; l'original du DevBlog Ankama est perdu | **Le DevBlog Ankama original a été retrouvé** (v1.27, 2010, archive web.archive.org du 18/11/2010) : `docs/knowledge/sources/S1-devblog-ankama-1.27.md`. Ce sont des chiffres d'**Ankama**, pas de relais. Cinq ancres, dont une **inédite, jamais relayée en quinze ans** : création d'effet au mieux = **32/50/18**. Le relais Yin-Yang donnait `34/50/16` pour l'ancre 2 : **faux**, l'original dit `43/50/7`. Le triplet `1/22/77` (« exo avec puits ») qu'il diffusait **n'existe ni en FR ni en DE** : c'est une fabrication. | `SOURCE PRIMAIRE — v1.27` pour les valeurs ; **transposition à Unity = `HYPOTHÈSE`** (argument en sa faveur : le 15 % et le 1 % sont reconduits à l'identique dans le tutoriel Unity actuel). Généalogie : `docs/knowledge/sources/genealogie.md`. Valeurs du dépôt **inchangées**, justifications réécrites. |
| 2026-09-10 | `src/logic/probability/types.ts` (`distanceToMax`) ; `models/officialFactorsLinear.ts` ; `empirical_params.json → probability.heavyExoEcShare` | `distanceToMax` renvoyait **0** pour toute ligne exotique, la rendant indiscernable d'une ligne au jet parfait. Avec `a = 0,15`, **un exo PA sur objet propre sortait à 15 / 0 / 85**, et le plancher de 1 % ne mordait jamais (0,15 > 0,01) | **Défaut corrigé (P2, priorité 1).** Le tutoriel Ankama Unity donne 1 % : le simulateur était **quinze fois trop optimiste sur l'opération la plus coûteuse du jeu**. `distanceToMax` renvoie désormais `null` quand la notion n'a pas de sens (exo, jet fixe). Un **garde-fou d'exotisme** (`src/logic/probability/exoGuard.ts`) précède tout modèle : exo PA/PM/PO **plafonné à 1 %** (donc épinglé à 1 %, plancher et plafond coïncidant) ; toute autre création d'effet renvoie un **INTERVALLE** borné par les ancres 4 et 5 (`32/50/18 → 1/0/99`), affiché comme tel, sans estimation ponctuelle. Le tirage d'issue utilise la **borne basse**, choix conservateur. **Aucun continuum 32 % → 1 % n'a été construit** : sa forme est inconnue et l'inventer produirait le chiffre faussement précis qu'on cherche à éviter. | `SOURCE PRIMAIRE — Unity` (valeur 1 %) ; l'usage en **plafond** est une décision de projet, le tutoriel disant littéralement « peut descendre jusqu'à ». Bornes de l'intervalle : `SOURCE PRIMAIRE — v1.27`, transposition `HYPOTHÈSE`. Tests : `src/__tests__/probability/exoGuard.test.ts` (13 tests, dont l'invariant sous paramètres adverses). |
| 2026-09-10 | `empirical_params.json → params.overCapWeight` ; `src/logic/engine/overCap.ts` | Une seule constante `overCapWeight = 101` servait **à la fois** à la borne par ligne (règle 1) et au cumul objet (règle 2, `overCapScope = global`) | **Deux plafonds distincts** dans le DevBlog 1.27, que le corpus communautaire avait fusionnés : (i) **par effet**, dont l'exemple est « 101 points de Force sur une base 60 » — 101 est un exemple, pas une constante nommée ; (ii) **par objet**, « une limite fixe de puissance d'effets non-naturels, pour l'intégralité des objets », celui qui interdit PA **et** PM — **valeur non donnée par Ankama**. Nouveau paramètre `params.objectNonNaturalCap`, **initialisé à 101** : comportement identique, tous les tests existants passent sans modification. Encadrement déduit **[100 ; 190[** (un exo PA seul passe donc ≥ 100 ; PA+PM impossible donc < 190), sous trois hypothèses explicitées dans la note. | `SOURCE PRIMAIRE — v1.27` (existence des deux plafonds) ; **`INCONNU`** (valeur du plafond objet). ⚠️ La **forme** du plafond par effet reste `CONTRADICTION` : Ankama borne une **somme de deux termes**, le code borne un seul terme — trois lectures L1/L2/L3, voir `arbitrages-2026-09-10.md` §4.1(i). Question ouverte : un exo PA et un exo PO coexistent-ils ? Si oui, la borne basse monte à 151 et 101 est réfuté. Tests : `src/__tests__/engine/capSplit.test.ts`. |
| 2026-09-10 | `src/logic/probability/models/officialFactorsLinear.ts` ; `empirical_params.json → officialFactorsLinear` | Le modèle n'utilisait que **deux** facteurs (distance au jet max, niveau), au motif que « les seuls facteurs cités par Ankama » étaient ceux-là | Le DevBlog en cite **six**, dans un ordre d'importance explicite, et le **premier** — la **qualité globale de l'objet, hors ligne visée** — était totalement absent. C'est lui, et non un réglage de `a`, qui explique que le modèle sorte **16/42/42** sur une ligne 49/50 là où l'ancre 2 donne **43/50/7** : le modèle ne pouvait pas distinguer l'ancre 2 (objet **simple**, ligne parfaite) de l'ancre 3 (objet **entièrement parfait**), qui ont la même distance sur la ligne visée. **Variable manquante, pas paramètre mal réglé.** Ajout du terme `e` (`src/logic/probability/itemQuality.ts`) et des cinq facteurs structurels en drapeaux (`probability.structuralFactors`), **tenus hors du vecteur ajusté** a/b/c/d/e. | `SOURCE PRIMAIRE — v1.27` (existence et rang de chaque facteur) ; **toutes les pentes à 0**, donc **aucune sortie ne change**. `c` : le sens (positif) et l'ordre de grandeur (faible) sont désormais connus. `d` : n'est plus « une proposition du projet sans source ». |
| 2026-09-10 | `empirical_params.json → probability.model` ; `src/logic/probability/models/devblog127.ts` | Aucun modèle ne reproduisait les ancres officielles | Nouveau modèle **`devblog_1_27`**, interpolation linéaire par morceaux des ancres 1 à 3 (D = 0 donne 66/34/0, D = 0,5 donne 43/50/7, D = 1 donne 15/50/35), **SN plafonné à 50 %** (« au maximum de 50 % »). Les ancres 4 et 5 sont couvertes par l'intervalle du garde-fou : les cinq ancres sont donc représentées, trois par la courbe et deux par l'intervalle. **Jamais le défaut**, et **son nom porte sa version** — un modèle appelé `devblog_1_27` ne peut pas être confondu avec une mesure Unity, ce qu'un nom comme « ancres officielles » aurait permis. | Courbe : `SOURCE PRIMAIRE — v1.27`, transposition `HYPOTHÈSE`. Poids de difficulté (`devblog127.difficultyWeights`) : **`INCONNU`**, seul leur ORDRE est sourcé. Tests : `src/__tests__/probability/devblog127.test.ts`. |
| 2026-09-10 | `src/logic/engine/losses.ts` (`removablePoints`) ; `src/logic/engine/applyRune.ts` ; `params.lossSelection.unpayableSn` | Les lignes de malus n'étaient **jamais** candidates à la perte (`max(0, value)` renvoie 0 pour une valeur négative) ; un SN impayable devenait un « échec sans effet », choix de projet sans source | Trois règles primaires v1.27 codées. (a) **Bonus : plancher à 0** — déjà le cas, désormais documenté. (b) **Malus : perte possible seulement s'il est overmaxé** (meilleur que son jet naturel), et **jamais au-delà du malus maximum naturel** — l'ancien comportement était voisin **par accident**, pas par règle ; un malus overmaxé est maintenant candidat, ce qui **change des sorties**. (c) **SN impossible : « rien ne se passe »** — aucun gain, aucune perte, reliquat intact ; nouveau défaut `no_effect`, `ec_no_effect` conservé en option. ⚠️ **Le DÉCLENCHEUR de 1.27 (« un AUTRE jet ») n'est pas repris littéralement** : il suppose que la ligne visée ne peut pas payer, ce que l'observation Unity du 2026-09-09 (rang R1) réfute. C'est la condition générale « si ce résultat n'est pas possible » qui est codée, mesurée avec les règles de paiement d'Unity. | `SOURCE PRIMAIRE — v1.27`. N'entre PAS : l'**épargne probabiliste** d'un bonus trop puissant — Ankama ne donne aucun chiffre, l'implémenter serait inventer une formule. Tests : `src/__tests__/engine/devblogLossRules.test.ts`. |
| 2026-09-10 | `src/logic/engine/losses.ts` (absorption par le reliquat) | La perte est absorbée par le reliquat **en priorité et en totalité** | **Aucun changement de comportement.** Un SEAM a été posé (`src/logic/engine/poolConsumption.ts`, `params.residualPool.poolConsumptionRule`) avec **une seule implémentation : le comportement actuel**. Les trois lectures alternatives de l'anomalie A1 (observation du 2026-09-10 : puits 0,2 disponible, rune de poids 1, EC donne −5 Vitalité et **puits inchangé**, là où le moteur prédisait −4 et puits à 0) ne sont **délibérément pas écrites**. | `INCONNU`, **N = 1**. Généraliser sur une occurrence introduirait un biais systématique sur toute session à puits non nul. Campagne requise : environ 20 EC sur objet à puits non nul, relevé avant/après. Converge avec le DevBlog : « absorbera **en partie** les échecs futurs ». |
| 2026-09-10 | `empirical_params.json → params.potions.damageKeptPercentByLevel` ; `src/components/atelier/ActionPanel.tsx` | Statut `INCONNU` ; l'interface affichait pourtant une pastille `CONTRADICTION` — **incohérence** entre la donnée et son affichage | Statut passé à **`CONTRADICTION`** : il ne s'agit pas d'une absence de source mais de **trois** sources incompatibles. V1 (R1, Unity 3.6.10.11) : deux paliers, aucun taux affiché. V2 (R5) : 50 / 65 / 80 %, Rétro/2.x. **V3 (R5), qui manquait à la note** : le palier 65 % supprimé en 3.1, armes converties à 85 % — affirmation distincte du « 85 % » de jeuxonline, qui décrivait la potion niveau 50 de Rétro. **Aucune valeur n'entre dans le code**, le module reste désactivé. | `CONTRADICTION NON RÉSOLUE`. Mesure qui tranche : dégâts d'une arme avant/après potion, par palier (20 et 80), sur deux armes de fourchettes différentes. Deux mesures par palier suffisent — le taux est une constante, pas une distribution. |
| 2026-09-10 | observation du 2026-09-10, anomalie **A2** ; `docs/knowledge/deltas-2026-09-10.md` | La source conclut à un **gain plafonné** (`6 Vitalité` affichés pour une Rune Pa Vi de +15) et en déduit que « le gain vaut toujours la valeur de la rune » est invalidé | **Lecture retenue contre la source** : SN ordinaire avec **affichage en net**. Perte de 3,0 = 9 Vita (1,8) + 12 Ini (1,2) donne Vitalité +15 − 9 = **+6 net**, Initiative **−12**, poids total **exactement** le poids de la rune, reliquat **0** donc aucune mention `± reliquat`. Les quatre observables sont reproduits par **deux règles déjà primaires**, sans mécanisme nouveau ; la lecture « gain plafonné » en exigerait un. Précédent inscrit le 2026-09-09 (SN Ra Vi : +50 brut, net +22). **Discriminant falsifiable** : une tentative à reliquat nul avant ET après dont le poids total perdu diffère du poids de la rune fait sauter cette lecture. | `INCONNU`, **N = 1**, aucun code modifié. **A3 sort du corpus exploitable** : son horodatage (t ≈ 32,0 s) est confondu avec celui d'A2 — lecture non fiable jusqu'à revue de la vidéo. |
| 2026-09-10 | `docs/dofus-forgemagie-recherche/` (bundle non versionné) | Corpus de recherche actif, calibré sur `1/22/77` (compté comme cible atteinte) et retenant `34/50/16` par défaut | **Archivé** dans `docs/archive/2026-09-10-dofus-forgemagie-recherche/` avec un `AVERTISSEMENT.md`, conformément à `CLAUDE.md` (archiver, ne pas effacer). Motif : tant qu'il restait en place, une session future pouvait le relire et se recontaminer. `docs/forgemagie-dropin/`, postérieur au DevBlog, intègre déjà les corrections et **reste en place**, mais reçoit son propre `AVERTISSEMENT.md` : il n'est pas audité, et son sous-dossier `_recherche/` porte encore des copies périmées du corpus contaminé. | `src/` n'a **jamais** été contaminé : `empirical_params.json` avait explicitement refusé ces triplets. La contamination était entièrement documentaire. |
| 2026-09-10 | `src/logic/probability/exoGuard.ts`, `constraints.ts`, `empirical_params.json → probability.heavyExoCharacteristics` ; interface de l'atelier | L'exo lourd était présenté comme `SOURCE PRIMAIRE` — « le taux d'un pour cent est écrit tel quel dans le tutoriel d'Ankama » | **Étiquetage corrigé le jour même, valeur inchangée.** Le raisonnement appliqué au PM et à la Portée ne l'avait pas été au PA. Ankama écrit que le taux « **peut descendre jusqu'à** 1 % » : c'est un **plancher atteignable**, pas la valeur du cas. Le moteur en fait aussi un **plafond**, et ce second pas est une **POLITIQUE** — exactement celle de `unknownIntervalSampling = worst`, appliquée au cas de l'exo lourd. Un exo lourd est une création d'effet comme une autre : son intervalle théorique va lui aussi de l'ancre 5 (1 %) à l'**ancre 4 (32 %)**, et **rien ne documente où le PA tombe entre les deux**. Le statut de l'estimation passe de `SOURCE PRIMAIRE` à `POLITIQUE` ; l'interface affiche désormais « hypothèse » et nomme la borne haute. La distinction PA / PM-PO est conservée : pour le PA le plancher est attesté, pour les deux autres Ankama ne les nomme même pas. | `SOURCE PRIMAIRE — Unity` : 1 % est **atteignable** en exo PA. `HYPOTHÈSE COMMUNAUTAIRE` forte : que ce soit **la** valeur — soutenue par le consensus des guides et par la densité de 100, la plus lourde du jeu, qui place l'opération à l'extrémité difficile. Aucun changement de comportement : le clamp reste à 1 %. |
| 2026-09-05 | `Reconstruction_…` (TL;DR, PARTIE 10, PARTIE 13 ligne K, tableau final) ; `audit-forgemagie-dofus3.md` §2 et §13 ; ancien `CLAUDE.md` | Les règles des runes de transcendance sont attribuées au **devblog 2.72** | La source primaire est le **devblog de la mise à jour 2.58** (https://www.dofus.com/fr/mmorpg/actualites/devblog/billets/1255546) : un objet ayant reçu une rune de transcendance ne peut plus être forgemagé **ni réinitialisé par un orbe**. Le devblog 2.72 concerne le **migrateur d'objets** (modification rétroactive des exemplaires existants), pas la définition des runes de transcendance. | `SOURCE PRIMAIRE` (verrou objet + orbes, codé en dur dans `src/logic/engine/transcendence.ts` et `orb.ts`). Le refus si over/exo et le taux par rang restent `HYPOTHÈSE COMMUNAUTAIRE` (`empirical_params.json → transcendence`). |
| 2026-09-05 | `Reconstruction_…` PARTIE 10 (formulation « verrouille l'objet ») vs décision de projet du 2026-09-05 (« ligne verrouillée ») | Verrou par ligne envisagé en phase 2 | Verrou de l'**objet entier**, conformément au devblog 2.58 ; le paramètre `transcendence.lockScope` créé provisoirement a été supprimé. | `SOURCE PRIMAIRE` |
| 2026-09-05 | `Reverse-Engineering_…` §A (« le reliquat lui-même n'est pas exposé au client », 2.x) | Reliquat invisible côté client | Un guide 2026 affirme que le reliquat est affiché dans l'onglet de forgemagie du client Unity. Non tranché. | `CONTRADICTION` (`empirical_params.json → residualPool.visibleInClient`), à vérifier en jeu |
| 2026-09-06 | `audit-forgemagie-dofus3.md` §7 et §13 (borne 101 par ligne, confiance C-D) vs `Reconstruction_…` PARTIE 6 (cumul global) | Portée de la borne d'over/exo non tranchée, défaut `per_line` provisoire | Le guide Huz (huzounet.fr, màj 21/01/2026) donne deux exemples qui additionnent deux lignes (« 10 ini et 1 PA », « 55 vita 1 PM » = 101) et conseille de ramener un over 213/200 vita à 200 avant un exo PA : lecture **cumulée** sur l'objet, mesurée sur la part over (valeur − max) et l'exo. Le défaut passe à `global`, `per_line` reste en option. Réfutation possible : un objet en jeu avec exo PA et over ≥ 2 de poids sur une autre ligne. | `HYPOTHÈSE COMMUNAUTAIRE` (`empirical_params.json → overCapScope`), testé dans `src/__tests__/engine/overCap.test.ts` |
| 2026-09-09 | `src/logic/probability/constraints.ts` (ancienne lecture) ; tutoriel Ankama /420190 | Plancher de 15 % appliqué à toute tentative non « exo lourd », y compris overmax et exo non lourd | **Mauvaise lecture d'une SOURCE PRIMAIRE**, corrigée : le texte dit « hors tentative d'overmax ou de forgemagie exotique ». Le plancher 15 % ne vaut que si la rune laisse la ligne ≤ jet max ET n'est pas exo ; over et exo non lourd : aucun plancher (0 autorisé, valeur INCONNUE, tout vient du modèle) ; exo lourd : 1 % inchangé. Nature de la tentative : `attemptKindOf`, tests `floorScope.test.ts`. | `SOURCE PRIMAIRE` (portée du plancher) ; over/exo `INCONNU` |
| 2026-09-09 | observations en jeu (Yanis, Costumager, Cape Bouffante, `data/observations/captures/2026-09-09/`) ; `src/logic/engine/losses.ts` | La ligne visée par la rune n'est jamais victime de la perte (exclusion `excludeCharacteristicId`) | SN Ra Vi : +50 brut, −28 vita −44 ini = 10,0, net +22 : la ligne visée perd, après application du gain. Exclusion supprimée, non paramétrable. | `SOURCE PRIMAIRE` (2 observations) |
| 2026-09-09 | mêmes observations ; `params.ecLossFactor` (supprimé) | Perte en EC = ecLossFactor × poids de la rune, INCONNU | EC = poids de la rune exactement (10,0 sur deux Ra Vi), répartie sur plusieurs lignes si besoin. Paramètre supprimé, règle codée en dur et testée. | `SOURCE PRIMAIRE` |
| 2026-09-09 | mêmes observations ; `applyRune.ts` | Perte non absorbable : poids « perdu » signalé, issue inchangée | L'EC retire tout ce qui reste puis s'arrête (−30 vita = 6,0 pour 10 demandés) ; à lignes et reliquat nuls, « Échec » sans effet, rune consommée. Un SN impayable n'a jamais été observé : `lossSelection.unpayableSn` (défaut `ec_no_effect`, INCONNU) ; en pratique inatteignable, le gain de la rune étant lui-même retirable. Avertissement « perte non absorbable » dans le Livre de forge. | `SOURCE PRIMAIRE` (EC) ; `INCONNU` (SN) |
| 2026-09-09 | mêmes observations ; `losses.ts` (quantité retirée = plus petit entier couvrant le poids) | Quantité perdue sur une ligne en SN | Quatre mesures : SN rune de 1 → 11 ini (1,1) et 12 ini (1,2) alors que 10 (1,0) suffisaient, excédent au reliquat ; EC Ra Vi → 10,0 pile (deux fois). La loi de quantité en SN est **INCONNUE** ; la règle de sélection de quantité n'est pas modifiée en attendant N observations. | `INCONNU` |
| 2026-09-09 | mêmes observations | — | Les 13 tentatives de la série restent à transcrire au format tentative v1 (`lineStateBeforeDeduced = true`, reliquat lu) : valeurs par tentative non encore reçues. Dossier de captures créé avec la liste des champs nécessaires. | en attente |
| 2026-09-09 | recherche HDV (Yanis) ; `data/dataset.json → transcendenceRunes` 20569 | Objets « 140 vita » (Cape Bouffante, max 40) et « 200 vita » (Abracapa Ancestrale, max 100) lus comme une limite d'over liée au niveau | Ce sont des **Rata Vi** (+100 vita, rune 20569) : statut « Empêche les futures forgemagies » affiché. Aucune source n'établit une limite d'over dépendant du niveau de l'objet : hypothèse écartée. | hypothèse écartée, tracée |
| 2026-09-09 | même recherche ; `data/observations/item-snapshots.json` | — | Cape du Wa Wabbit (971, niveau 60) vue à 233 vita pour un jet max de 100, autres lignes au max, sans transcendance : over classique de 26,6 de poids obtenu par succès critiques. Preuve que des SC en over existent, sans plafond lié au niveau. | `SOURCE PRIMAIRE` (objet vu) |
| 2026-09-08 | observation en jeu (Yanis, client 3.6.10.11, `data/observations/captures/2026-09-08/`) ; `residualPool.visibleInClient` | Reliquat invisible côté client (docs 2.x) vs affiché (Huz 2026) | Le client Unity affiche « reliquat : 0,1 » dans l'interface Costumager et une entrée « + reliquat » dans l'historique. **Contradiction fermée** : `visibleInClient = true`. | `SOURCE PRIMAIRE` |
| 2026-09-08 | même observation ; `data/observations/observations.json` (Cape Bouffante 2414, Rune Vi, SN) | Vitalité 0,2 et Initiative 0,1 : HYPOTHÈSE COMMUNAUTAIRE | Perte de 11 initiative = 1,1 de poids pour une rune Vi de 1, reliquat affiché 0,1 : l'arithmétique confirme 0,1/pt et 0,2/pt. État AVANT déduit de l'après et des modifications (champ `lineStateBeforeDeduced`). | `SOURCE PRIMAIRE` (densités 11 et 44) |
| 2026-09-08 | même observation | Perte = plus petit nombre entier de points couvrant le poids (règle codée dans `src/logic/engine/losses.ts`, arrondi supérieur) | Le jeu a retiré **11** initiative (1,1) alors que 10 (1,0) auraient couvert la rune (1). La quantification de la perte n'est donc **pas** le minimum entier : loi INCONNUE (multiple de la rune Ini ? tirage ? arrondi différent ?). Une seule observation ; à reproduire avant de toucher le moteur. | `INCONNU` (règle du moteur à revoir sur N observations) |
| 2026-09-08 | infobulles en jeu ; `densities.{27,28,82,83,69,70}` | Esquive/Retrait PA-PM 7, Puissance pièges 2 (INCONNU), Dommages pièges 5 : non revérifiés Unity | Lus dans l'infobulle des runes normales +1 (`data/observations/tooltips/tooltips.json`) : 7, 7, 7, 7, 2, 5. Valeurs 2.x conservées sur Unity ; Puissance pièges confirmée. | `SOURCE PRIMAIRE` |
| 2026-09-08 | infobulles en jeu ; `densities.121` et `densities.124` | % Résistance mêlée / distance = 15 (Millenium, dofastuces, KamelAkar) | Lues **10** dans l'infobulle Unity (Rune Ré Per Mé, Rune Ré Per Di). Valeur changée 15 → 10. **Deuxième divergence Unity vs tables 2.x** après Renvoi de dommages (5 vs 10) ; Icksir (10) avait raison. Plafonds dérivés recalculés automatiquement (floor(101/10) = 10). | `SOURCE PRIMAIRE` |
| 2026-09-08 | client Unity + `data/dataset.json → potions` ; `params.potions.damageKeptPercentByLevel` | Potions : 50/65/80 % (wiki 2.x) vs 85 % (Rétro), trois paliers | L'infobulle Unity ne montre aucun taux ; le dataset n'a que deux paliers (niveau 20 et 80), le palier 50 des tables 2.x n'existe pas. Table 50/65/80 retirée du défaut (valeur vide). | `INCONNU`, mesure en jeu requise |
| 2026-09-08 | recherche HDV (Yanis, 2026-09-08) ; `overCapScope`, `overCapLineBasis` | CONTRADICTION (deux lectures de Huz) | Aucun contre-exemple trouvé en HDV (ni exo PA avec over ≥ 2 ailleurs, ni ligne over à plus de 101 de poids total). Non réfuté ≠ prouvé : les deux paramètres repassent en hypothèse avec la mention « non réfuté par recherche HDV 2026-09-08 ». | `HYPOTHÈSE COMMUNAUTAIRE` |
| 2026-09-08 | `X` = `2026-09-07-croisement-densites-reliquat.md` (rapport externe, document source) vs errata du 2026-09-06 | Portée de la borne 101 : X §6 lit Huz « par ligne / statistique », l'errata du 06/09 lit « cumul objet » (exemples « 10 ini et 1 PA », « 55 vita 1 PM », conseil de lisser un over avant un exo) | Deux lectures de la même phrase. Défaut `overCapScope = global` **conservé**, statut passé à `CONTRADICTION`. Tranchée par observation d'objet en HDV : un objet portant un exo PA (100) ET un over ≥ 2 de poids sur une autre ligne (ex. 211/200 vita) réfute le cumul ; l'impossibilité répétée d'en trouver le soutient. Schéma : `data/observations/item-snapshot.schema.json`. | `CONTRADICTION` (`empirical_params.json → overCapScope`) |
| 2026-09-08 | X §2-§3 ; `empirical_params.json → densities.18` | % Critique : 10 (tables 2.x) vs 30 (Xixou, Alterya) traité comme CONTRADICTION | Écart de **version** : 30 = Rétro 1.29/1.49, 10 = 2.x et Dofus 3 (« sur 2.0 les runes critique ont un poids de 10 alors que sur 1.29 c'est 30 »). 10 lu en jeu Unity par un joueur du projet. | `SOURCE PRIMAIRE` (lecture en jeu ; à consigner dans `data/observations/tooltips/`) |
| 2026-09-08 | X §2-§3 ; `densities.50` | Renvoi de dommages : 10 (Millenium + lilgallon, 2.x) | 5 lu en jeu Unity ; la convergence 10/10 des tables 2.x n'est pas une preuve (recopie probable, X §4). Valeur 5 conservée. | `CONTRADICTION` maintenue jusqu'à N tentatives (X §8) |
| 2026-09-08 | X §3 ; `densities.11` | Vitalité « 0,25 » débattu | Confusion avec la densité des **Pods** (0,25/pt) ; 0,2 confirmé par dofastuces (« 5 vita = 1 de poids ») et par la borne 505 × 0,2 = 101. | `HYPOTHÈSE COMMUNAUTAIRE` (0,2) |
| 2026-09-08 | X §2, §4 ; `densities.{27,28,82,83,33-37,120,122,123,125,25,54-58,85,87}` | Esquive/Retrait PA-PM 7, % rés. élém. 6, % dommages 15, Puissance 2, résistances fixes 2 | Toutes issues de la racine 2.x (Millenium 2018 / dofastuces), recopie probable entre guides, **non revérifiées sur Unity** ; % rés. mêlée/distance 15 : de plus INCONNU si conservé, désaccord KamelAkar 15 / Icksir 10 entier ; Puissance pièges 2 : aucune source ni test. | `HYPOTHÈSE COMMUNAUTAIRE` (mention ajoutée à chaque note) ; 121/124 `CONTRADICTION` ; 69 `INCONNU` |
| 2026-09-08 | X §6 ; `residualPool.visibleInClient` | Reliquat invisible côté client (2.x) | Citation Huz 2026 ajoutée : « Aujourd'hui, nous n'avons plus besoin de calculer ce Reliquat, il s'affiche directement sur l'onglet de forgemagie ». Le tutoriel Ankama dit l'inverse (ancien). | `CONTRADICTION` jusqu'à une capture d'écran Unity versionnée |
| 2026-09-08 | X §6 ; `notes[]` potions | Potions : 50/65/80 % vs 85 % « post-3.1 » | Localisé par version : 50/65/80 % = wiki-dofus (Rétro/2.x), 85 % = Rétro (jeuxonline 14787, potion niveau 50 « ne réduit les dégâts que de 15 % »). Aucun changelog 3.x. | `CONTRADICTION` pour Unity |
| 2026-09-08 | X §3 (chronologie officielle) | — | **2.29** (jeuxonline 48173) : « Le poids en forgemagie de certains effets est modifié », liste des effets touchés non reproduite → INCONNU ; **2.58** (jeuxonline 58753) : « Le "poids" des runes est désormais affiché dans l'infobulle de l'objet » ; **aucun changelog 3.x** modifiant une densité n'a été trouvé. | `SOURCE PRIMAIRE` pour les deux changelogs (relais jeuxonline d'Ankama) ; liste 2.29 `INCONNU` |
| 2026-09-08 | X §7 (datamining, résultat négatif) | — | Aucun champ de densité de forgemagie dans api.dofusdb.fr, doduapi (`int_minimum`/`int_maximum` seulement), DDC (non exclu à 100 %, JSON brut non inspecté) ni Datafus ; `realWeight` = pods d'inventaire. KamelAkar code ses densités en dur malgré l'API : preuve directe de l'absence. Seule lecture possible : infobulle en jeu (2.58+) → `data/observations/tooltips/`. | résultat négatif consigné ; `data/README.md` aligné |
| 2026-09-08 | ancien code (`applyRune.ts` : refus entier de toute rune dépassant la borne) | Une rune qui dépasserait la borne d'over/exo est refusée | Lecture d'un joueur expérimenté : la rune **passe et s'arrête à la borne** (Ra Vi sur 480 vita → 505), l'excédent est perdu ; refus seulement si plus rien ne peut s'appliquer. Paramètre `overCapExcess.behaviour` (`truncate` défaut, `refuse` option). Le poids retenu pour la perte d'une rune tronquée est `overCapExcess.lossBasis` (`full_rune` défaut, `applied_only`), INCONNU. | `HYPOTHÈSE COMMUNAUTAIRE` / `INCONNU` (`empirical_params.json → overCapExcess`) |
| 2026-09-06 | ancien code (`overCap.ts`, `statCaps.ts` : 101 de poids **au-delà** du jet max, soit 905 vita sur une base 400) | Borne par ligne mesurée sur la part over | Lecture d'un joueur expérimenté de la règle Huz « 505 vita, 101 agilité » : c'est la **valeur totale** de la ligne qui est bornée, quelle que soit la base (base 400 vita : +105 ; base 500 : +5 ; 40 sagesse = 120 : plus d'over possible). Paramètre `overCapLineBasis` (`total_value` défaut, `over_part` en option). Réfutation : une ligne observée au-dessus de son jet max avec plus de 101 de poids total. | `HYPOTHÈSE COMMUNAUTAIRE` (`empirical_params.json → overCapLineBasis`) |
| 2026-09-06 | aucun rapport (proposition du projet) | — | Ajout d'un terme « usage de la borne over/exo après la rune » au modèle `official_factors_linear` (coefficient `d`), d'après l'observation que les objets proches de la borne sont rares et difficiles à finir. Pente nulle par défaut : le modèle par défaut ne change pas tant que le journal d'observations n'a pas mesuré cette pente. | `INCONNU` (`empirical_params.json → probability.officialFactorsLinear.d`) |
| 2026-09-06 | `Reconstruction_…` PARTIE 10 (orbe = « jet de craft aléatoire ») ; ancien `src/logic/engine/orb.ts` (tirage uniforme codé en dur, « faute de mieux ») | Loi du jet de craft implicitement uniforme | La loi du jet de craft n'est publiée nulle part : elle devient un paramètre `craft.rollDistribution` (défaut `uniform`, alternative `triangular` sans source, pour test), lu par `src/logic/craft/rollItem.ts` et partagé par l'orbe régénérant et la saisie rapide « Jet aléatoire » de l'atelier. Un relevé de jets bruts (journal d'observations) pourrait la départager. | `INCONNU` (`empirical_params.json → craft.rollDistribution`) |

Données client à l'appui (dataset DofusDB 3.6.10.11, `data/dataset.json → transcendenceRunes`) : chaque rune de transcendance porte l'effet **2825 « Empêche les futures forgemagies »**, l'effet 2826 (libellé vide) et l'effet **2827 « … % de chances de réussite »** dont la valeur est renvoyée à 0 par l'API. Les taux par rang (Ta / Pata / Rata) sont donc à extraire du client, pas de l'API.
